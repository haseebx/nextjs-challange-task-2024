/** @type {import('next').NextConfig} */
const nextConfig = {
    compiler: {
        reactStrictMode: true,
        compiler: {
          styledComponents: true,
        },
    }
};

export default nextConfig;
